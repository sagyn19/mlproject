# ML Project

## Installation

To install the project and its dependencies, run:

```bash
pip install -r requirements.txt
```

This will install the project in development mode, allowing you to import modules from the `src` package.

## Usage

After installation, you can import modules from the project:

```python
from src.exception import CustomException
from src.logger import logging
```
